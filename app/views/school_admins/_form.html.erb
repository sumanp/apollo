<%= form_with(url: school_school_admins_path) do |form| %>
  <% if school_admin.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(school_admin.errors.count, "error") %> prohibited this school_admin from being saved:</h2>

      <ul>
        <% school_admin.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.collection_select :user_id, User.order(:email),:id, :email, include_blank: true %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
